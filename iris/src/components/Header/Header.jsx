import "./Header.scss";
import logo from "/src/assets/logo/iris-logo.svg"

function Header(){
    <header>
        <nav>
            <img src={logo} alt="website logo"/>
        </nav>
    </header>

}

export default Header;